Generated via:

    openssl pkcs12 -export -inkey server_private.pem -in server_certificate.pem -out server_keystore.p12
    openssl pkcs12 -export -inkey server_private.pem -in server_certificate.pem -out server_keystore.p12
    openssl pkcs12 -export -inkey server_private.pem -in server_certificate.pem -out server_keystore.p12
